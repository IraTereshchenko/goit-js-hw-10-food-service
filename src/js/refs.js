const refs = {
  themeSwitchRef: document.querySelector('#theme-switch-toggle'),
  bodyRef: document.querySelector('body'),
  menuRef: document.querySelector('.js-menu'),
};

export const { themeSwitchRef, bodyRef, menuRef } = refs;